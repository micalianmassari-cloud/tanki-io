{
  "name": "tanki-io-server",
  "version": "1.0.0",
  "description": "TANKI.IO — WebSocket multiplayer server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=14"
  },
  "render": {
    "port": 3000,
    "healthCheckPath": "/health",
    "healthCheckTimeout": 10
  }
}
